/**
 * @chariot/board — Global Planner 占位
 * 外层 Planner 做全局冲突检测与排程提示
 * 完整实现由 Tia 后续负责
 */
import { Placeholder } from "@chariot/ui";

export function GlobalPlannerOverlay() {
  return (
    <div style={{ padding: "8px" }}>
      <Placeholder label="Global Planner Overlay (Tia)" />
    </div>
  );
}
